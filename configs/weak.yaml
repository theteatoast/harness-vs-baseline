# Weak-model probe: a small open model. Non-reasoning, so no reasoning block.
model: meta-llama/llama-3.1-8b-instruct
temperature: 0.2
target: synthapp

budgets:
  max_steps: 40
  max_tokens: 300000
  wall_clock_s: 900

ablation:
  planner: true
  verifier: true

cache: true
dry_run: false
